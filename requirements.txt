# requirements.txt - Project skeleton
#
# Use this file to list production dependencies (one per line) and pin
# versions when you're ready. Lines starting with `#` are ignored by pip
# and are useful as section headers or examples.
#
# Format examples:
#   package==1.2.3    # pin exact version
#   package>=1.2      # minimum version
#   -r other.txt      # include another requirements file
#   -c constraints.txt # apply constraint file
#
# Fill in the sections below with packages and versions.

# ---- Core dependencies ----------------------------------------------------
# e.g.
# flask==2.2.3
# requests==2.31.0

# Add core runtime packages here (web framework, HTTP clients, etc.)

# ---- Data / Science ------------------------------------------------------
# numpy==
# pandas==

# ---- Database / ORM ------------------------------------------------------
# sqlalchemy==

# ---- Testing (CI / dev) --------------------------------------------------
# pytest==
# pytest-cov==

# ---- Linting / Formatting / Type checking (dev) --------------------------
# black==
# flake8==
# mypy==

# ---- Dev tools -----------------------------------------------------------
# ipython==
# pre-commit==

# ---- Deployment / Servers -----------------------------------------------
# gunicorn==
# uvicorn==

# ---- Optional / Extras ---------------------------------------------------
# celery==
# redis==

# ---- Constraints / includes ----------------------------------------------
# If you use a constraints file, reference it here:
# -c constraints.txt
# To include a separate dev file:
# -r requirements-dev.txt

# ---- Helpful notes ------------------------------------------------------
# - When ready, pin exact versions using `package==x.y.z` for reproducible
#   installs and CI builds.
# - Consider maintaining a `requirements-dev.txt` for testing/linting.
# - Optionally use `pip-tools` (`pip-compile`) to generate pinned files.

